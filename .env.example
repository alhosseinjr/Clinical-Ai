# Base model ID (HuggingFace or local path)
BASE_MODEL_ID=Qwen/Qwen2.5-3B-Instruct

# Path to the LoRA adapter (if not using a merged model)
LORA_ADAPTER_PATH=models/clinical-lora-adapter-3b

# Path to the merged standalone model (highest priority if it exists)
MERGED_MODEL_PATH=models/clinical-3b-merged
